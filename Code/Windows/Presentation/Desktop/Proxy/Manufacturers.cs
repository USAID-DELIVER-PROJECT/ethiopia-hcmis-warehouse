
// Generated by MyGeneration Version # (1.3.0.9)

using System.Collections.Generic;

namespace Proxy
{
	public class Manufacturers 
	{
	
    //#region Web Service Getters
		
    //    public static List<Proxy.Manufacturer> GetAll()
    //    {
    //        BLL.Manufacturers v = new BLL.Manufacturers();
    //        v.LoadAll();
    //        return ToList(v);
    //    }

    //    public static List<Proxy.Manufacturer> GetUpdatesAfter(int LastVersion)
    //    {
    //        BLL.Manufacturers v = new BLL.Manufacturers();
    //        v.LoadUpdatesAfter( LastVersion );
    //        return ToList(v);
    //    }
		
    //#endregion
	
	
	
    //#region Utilities
	
    //    public static List<Proxy.Manufacturers> ToList(BLL.Manufacturers v){
    //        List<Proxy.Manufacturers> list = new List<Proxy.Manufacturers>();
    //        while (!v.EOF)
    //        {
    //            Proxy.Manufacturers t = new Proxy.Manufacturers();
    //          if(!v.IsColumnNull("ID"))
    //                 t.ID = v.ID;
    //          if(!v.IsColumnNull("Name"))
    //                 t.Name = v.Name;
    //          if(!v.IsColumnNull("CountryOfOrigin"))
    //                 t.CountryOfOrigin = v.CountryOfOrigin;
    //          if(!v.IsColumnNull("PFSAManufCode"))
    //                 t.PFSAManufCode = v.PFSAManufCode;
    //          if(!v.IsColumnNull("Phone"))
    //                 t.Phone = v.Phone;
    //          if(!v.IsColumnNull("Address"))
    //                 t.Address = v.Address;

    //            list.Add(t);
    //            v.MoveNext();
    //        }
    //        return list;
    //    }
	
    //    #endregion
		
		
		#region Web service Saving

        public static void SaveList(List<Proxy.Manufacturer> list)
        {
            BLL.Manufacturers bv = new BLL.Manufacturers();
            foreach (Proxy.Manufacturer v in list)
            {
                // try to load by primary key
                bv.LoadByPrimaryKey(v.ID.Value);
                // if the entry doesn't exist, create it
                if (bv.RowCount == 0)
                {
                    bv.AddNew();
                }
                // populate the contents of v on the to the database list
				              if( v.ID.HasValue )
				   bv.ID = v.ID.Value;
              if( v.Name != "" && v.Name != null )
				   bv.Name = v.Name;
              if( v.CountryOfOrigin != "" && v.CountryOfOrigin != null )
				   bv.CountryOfOrigin = v.CountryOfOrigin;
              if( v.PFSAManufCode != "" && v.PFSAManufCode != null )
				   bv.PFSAManufCode = v.PFSAManufCode;
              if( v.Phone != "" && v.Phone != null )
				   bv.Phone = v.Phone;
              if( v.Address != "" && v.Address != null )
				   bv.Address = v.Address;

                bv.Save();
            }


        }
	
	#endregion
	}
}
