
// Generated by MyGeneration Version # (1.3.0.3)

using System;
using System.Collections.Generic;

namespace Proxy
{
	public class StoreGroupDivision 
	{
	
	#region Properties
	
		private  int? _ID;
		public  int? ID
	    {
			get
	        {
				return _ID;
			}
			set
	        {
				_ID = value;
			}
		}

		private  string _Name;
		public  string Name
	    {
			get
	        {
				return _Name;
			}
			set
	        {
				_Name = value;
			}
		}

		private  string _Descriptiont;
		public  string Descriptiont
	    {
			get
	        {
				return _Descriptiont;
			}
			set
	        {
				_Descriptiont = value;
			}
		}

		private  int? _StoreGroupID;
		public  int? StoreGroupID
	    {
			get
	        {
				return _StoreGroupID;
			}
			set
	        {
				_StoreGroupID = value;
			}
		}


	#endregion
	
	
	#region Utilities
	
		public static List<StoreGroupDivision> ToList(BLL.SubAccount v){
			List<StoreGroupDivision> list = new List<StoreGroupDivision>();
            while (!v.EOF)
            {
                StoreGroupDivision t = new StoreGroupDivision();
              if(!v.IsColumnNull("ID"))
				    t.ID = v.ID;
              if(!v.IsColumnNull("Name"))
				    t.Name = v.Name;
              if(!v.IsColumnNull("Descriptiont"))
				    t.Descriptiont = v.Descriptiont;
              if(!v.IsColumnNull("StoreGroupID"))
				    t.StoreGroupID = v.StoreGroupID;

				list.Add(t);
                v.MoveNext();
            }
            return list;
		}
	
		#endregion
		
		
		#region Web service Saving
	
		public static void SaveList(List<HCMIS.Desktop.DirectoryServices.SubAccount> list)
        {
            BLL.SubAccount bv = new BLL.SubAccount();
            foreach (HCMIS.Desktop.DirectoryServices.SubAccount v in list)
            {
                // try to load by primary key
                bv.LoadByPrimaryKey(v.ID.Value);

                // if the entry doesn't exist, create it
                if (bv.RowCount == 0)
                {
                    bv.AddNew();
                }
                // populate the contents of v on the to the database list
              if( v.ID.HasValue )
				   bv.ID = v.ID.Value;
              if( v.Name != "" && v.Name != null )
				   bv.Name = v.Name;
              if( v.Description != "" && v.Description != null )
				   bv.Descriptiont = v.Description;
              if( v.AccountID.HasValue )
				   bv.StoreGroupID = v.AccountID.Value;

                bv.Save();
            }


        }
	
	public static void DeleteList(List<int> list)
        {
            BLL.SubAccount bv = new BLL.SubAccount();
            foreach (int v in list)
            {
                // try to load by primary key
                bv.LoadByPrimaryKey(v);
                // if the entry doesn't exist, create it
                if (bv.RowCount > 0)
                {
                    bv.MarkAsDeleted();
					bv.Save();
                }
                // populate the contents of v on the to the database list

            }


        }
	
	
	#endregion
	}
}
